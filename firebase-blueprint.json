{
  "entities": {
    "Submission": {
      "title": "Submission",
      "description": "A document submission for cash advance or reimbursement.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "type": { "type": "string", "enum": ["uang_muka", "reimburse", "laporan_uang_muka", "pembiayaan"] },
        "title": { "type": "string" },
        "amount": { "type": "number" },
        "description": { "type": "string" },
        "status": { "type": "string" },
        "currentStageIndex": { "type": "number" },
        "picName": { "type": "string" },
        "sumberRekening": { "type": "string", "enum": ["SMP", "SMA"] },
        "kodeBudget": { "type": "string" },
        "noDokumen": { "type": "string" },
        "submittedBy": { "type": "string" },
        "submittedByName": { "type": "string" },
        "submittedByEmail": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" },
        "evidenceUrl": { "type": "string" },
        "history": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "stage": { "type": "string" },
              "status": { "type": "string" },
              "actor": { "type": "string" },
              "timestamp": { "type": "string", "format": "date-time" },
              "comment": { "type": "string" }
            }
          }
        }
      },
      "required": ["type", "title", "amount", "status", "currentStageIndex", "submittedBy", "createdAt"]
    },
    "User": {
      "title": "User",
      "description": "Application user profile.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string" },
        "displayName": { "type": "string" },
        "photoURL": { "type": "string" },
        "role": { "type": "string", "enum": ["staff", "finance", "accountant", "management", "admin"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "email", "role"]
    },
    "AppConfig": {
      "title": "AppConfig",
      "description": "Application configuration settings.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "logoURL": { "type": "string" }
      },
      "required": ["id"]
    },
    "BerkasDigital": {
      "title": "BerkasDigital",
      "description": "Google Drive links for digital files organized by month and year.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "tahun": { "type": "string" },
        "bulan": { "type": "string" },
        "link": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["tahun", "bulan", "link", "createdAt"]
    },
    "Donation": {
      "title": "Donation",
      "description": "Donation confirmation records.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "donaturName": { "type": "string" },
        "contact": { "type": "string" },
        "amount": { "type": "number" },
        "targetAccount": { "type": "string" },
        "notes": { "type": "string" },
        "status": { "type": "string", "enum": ["pending", "verified", "rejected"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": [
        "donaturName",
        "contact",
        "amount",
        "targetAccount",
        "status",
        "createdAt"
      ]
    }
  },
  "firestore": {
    "/submissions/{submissionId}": {
      "schema": "Submission",
      "description": "Collection of document submissions."
    },
    "/users/{userId}": {
      "schema": "User",
      "description": "User profiles and roles."
    },
    "/config/{configId}": {
      "schema": "AppConfig",
      "description": "Application configuration settings."
    },
    "/berkas_digital/{berkasId}": {
      "schema": "BerkasDigital",
      "description": "Collection of digital file links."
    },
    "/donations/{donationId}": {
      "schema": "Donation",
      "description": "Collection of donation confirmations."
    }
  }
}
